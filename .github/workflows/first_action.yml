name: First Workflow

on: workflow_dispatch

jobs:
  firstjob:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: echo "Hello i am bijoy"

      - name: Print goodbye
        run: echo "Goodbye"
